# Core
numpy>=1.24
pandas>=2.0
Pillow>=10.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# Deep learning (install torch per https://pytorch.org for your platform;
# on Apple Silicon the default pip wheel includes MPS support)
torch>=2.1
torchvision>=0.16

# Advanced / optional — pipeline degrades gracefully if these are absent
xgboost>=2.0          # falls back to sklearn HistGradientBoosting if missing
umap-learn>=0.5       # UMAP embedding in EDA (t-SNE always available)

# Notebook
jupyter>=1.0
nbformat>=5.9
